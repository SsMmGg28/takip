// Sayfa "use client" olduğundan metadata bu layout'tan verilir.
export const metadata = { title: "Şifre Belirle" };

export default function SetPasswordLayout({ children }: { children: React.ReactNode }) {
  return children;
}
